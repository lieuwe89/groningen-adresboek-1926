# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.pytest_cache/
*.egg-info/

# Project secrets — NEVER commit
.env
.env.*
!web/.env.local.example
fly.toml

# Pipeline — lives in its own repo (https://github.com/lieuwe89/tables-ocr-pipeline)
pipeline/
requirements.txt
scripts/
viewer.py
viewer/
Dockerfile.dgx

# Documentation & Markdown
*.md
!/README.md
docs/

# Generated pipeline outputs (rebuildable from cached OCR + LLM raw + scans)
# `output/overrides/` contains user-edited corrections and IS tracked.
output/*
!output/overrides/

# Large source assets — not in git (Drive / external transfer)
scans/
Maps/
tessdata/

# Drive sync transient state
.tmp.driveupload/
.tmp.drivedownload/
.tmp.*

# Editor / tooling
.claude/
.idea/
.vscode/

# Node — web/
node_modules/
.next/
out/
*.tsbuildinfo
web/next-env.d.ts

# Generated SQLite (rebuild via `npm run build:db` or `python scripts/build_db.py`)
web/data/

# Build artifacts
*.log
*.pid

# Local Claude task scratch
/tasks/
web/public/maps/
web/public/tiles/
