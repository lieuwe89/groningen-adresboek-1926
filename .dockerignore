# Large assets — not baked into the image (live on a Fly volume)
public/tiles/
public/maps/
public/scans/
**/public/tiles/**
**/public/maps/**
**/public/scans/**

# Generated / local-only
data/
.env.local
node_modules/
.next/

# Pipeline (separate repo)
_pipeline/
**/_pipeline/**
pipeline/
# scripts/ kept in build context — apply-overrides.mjs is COPYed into runner.
# Python scripts are dev-only and harmless to leave in the builder stage.
scans/
Maps/
output/
tessdata/
requirements.txt

# Misc
.git/
.claude/
*.log
*.pid
